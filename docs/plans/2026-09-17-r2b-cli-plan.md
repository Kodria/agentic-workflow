# R2-B CLI routing implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development`
> or `executing-plans` to implement one admitted serial compact slice at a time.

**Goal:** Add opt-in provider-neutral routing with approved policies and durable actual-dispatch evidence.
**Architecture:** Extend the existing CLI authorities; native platforms dispatch resolved envelopes.
**Tech Stack:** TypeScript, Node, Jest, existing native durable filesystem primitives.
**Modo de ejecución:** interactivo
**Issue:** https://github.com/Kodria/agentic-workflow/issues/126
**Base:** origin/main a3e2e2d4; CLI 9.8.0, baseline v4.1.0.
**Status:** implementation completed on the issue branch; automated evidence is
recorded in commits. Native/provider acceptance remains UNTESTED and requires
the dedicated acceptance playbook.
**Contract:** `docs/plans/2026-09-17-r2b-routing-contract.md` is the complete proposed supplement.
**Planning amendment:** Pending Claude R2-A trial does not block authoring this plan. Native routing acceptance remains separate.

<!-- AWM:COMPACT-SLICES:START v1 -->
{
  "schema": "compact-slices/v1",
  "planId": "issue-126-r2b-cli",
  "requirements": [
    "RF-4.5",
    "RNF-T.2",
    "RNF-T.3",
    "RF-4.2",
    "RF-4.3",
    "RF-4.4",
    "RNF-T.1",
    "RNF-T.5",
    "RF-4.1",
    "RF-5.2",
    "RNF-T.4",
    "RF-6.1",
    "RF-6.2",
    "RNF-T.6",
    "RNF-T.7"
  ],
  "sources": [
    {
      "id": "SRC-PROTOCOL",
      "path": "docs/plans/2026-09-17-r2b-routing-contract.md",
      "locator": "## Data contracts",
      "fact": "Defines complete proposed policy, runtime, envelope, commands, resolution and durable recovery contracts."
    },
    {
      "id": "SRC-PATHS",
      "path": "cli/src/core/paths.ts",
      "locator": "export function awmHome",
      "fact": "Resolve operator storage at call time without fixed machine paths."
    },
    {
      "id": "SRC-ATOMIC",
      "path": "cli/src/core/atomic-file.ts",
      "locator": "export function writeFileAtomicDurable",
      "fact": "Durable atomic replacement requires additional exclusive locking and CAS validation; rename alone is not CAS."
    },
    {
      "id": "SRC-ADMISSION",
      "path": "cli/src/core/admission/index.ts",
      "locator": "export async function admitPlan",
      "fact": "Reuse ordered plan/provider/currentness/sensors/journal/capability gates and retain six-target exhaustive table."
    },
    {
      "id": "SRC-PLAN",
      "path": "cli/src/core/plan/types.ts",
      "locator": "export interface PlanSlice",
      "fact": "V1 has exact slice fields; v2 must add a discriminated profile without changing v1."
    },
    {
      "id": "SRC-REQUESTS",
      "path": "cli/src/core/journal/requests.ts",
      "locator": "export function emitRequest",
      "fact": "Generation-fenced idempotent requests persist before supervisor application; emission alone is not applied custody."
    }
  ],
  "commands": [
    {
      "id": "CMD-POLICY",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "test",
        "--",
        "--runTestsByPath",
        "tests/core/model-policy/policy.test.ts",
        "tests/core/model-policy/store.test.ts",
        "tests/commands/model-policy/index.test.ts",
        "tests/core/secure-fs/native-bridge.test.ts"
      ],
      "covers": [
        "RF-4.5",
        "RNF-T.2",
        "RNF-T.3"
      ]
    },
    {
      "id": "CMD-RESOLVE",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "test",
        "--",
        "--runTestsByPath",
        "tests/core/model-policy/capabilities.test.ts",
        "tests/core/model-policy/resolve.test.ts"
      ],
      "covers": [
        "RF-4.2",
        "RF-4.3",
        "RF-4.4",
        "RNF-T.1",
        "RNF-T.5"
      ]
    },
    {
      "id": "CMD-PLAN",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "test",
        "--",
        "--runTestsByPath",
        "tests/core/plan/validate.test.ts",
        "tests/core/admission/index.test.ts",
        "tests/commands/plan/index.test.ts"
      ],
      "covers": [
        "RF-4.1",
        "RF-5.2",
        "RNF-T.4"
      ]
    },
    {
      "id": "CMD-JOURNAL",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "test",
        "--",
        "--runTestsByPath",
        "tests/core/model-policy/journal.test.ts",
        "tests/core/journal/types.test.ts",
        "tests/commands/watch/apply.test.ts",
        "tests/commands/job/gate.test.ts",
        "tests/commands/job/routing.test.ts"
      ],
      "covers": [
        "RF-6.1",
        "RF-6.2",
        "RNF-T.6",
        "RNF-T.7"
      ]
    },
    {
      "id": "CMD-TYPES",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "run",
        "typecheck"
      ],
      "covers": [
        "RF-4.5",
        "RNF-T.2",
        "RNF-T.3",
        "RF-4.2",
        "RF-4.3",
        "RF-4.4",
        "RNF-T.1",
        "RNF-T.5",
        "RF-4.1",
        "RF-5.2",
        "RNF-T.4",
        "RF-6.1",
        "RF-6.2",
        "RNF-T.6",
        "RNF-T.7"
      ]
    },
    {
      "id": "CMD-NATIVE-TEST-BUILD",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "run",
        "native:test-build"
      ],
      "covers": [
        "RF-4.5",
        "RNF-T.2",
        "RNF-T.3"
      ]
    },
    {
      "id": "CMD-NATIVE-FAULT",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "test",
        "--",
        "--runTestsByPath",
        "tests/core/secure-fs/native-bridge.test.ts"
      ],
      "covers": [
        "RF-4.5",
        "RNF-T.2",
        "RNF-T.3"
      ]
    },
    {
      "id": "CMD-ALL",
      "program": "npm",
      "args": [
        "--prefix",
        "cli",
        "test"
      ],
      "covers": []
    }
  ],
  "slices": [
    {
      "id": "C1",
      "title": "Approved portable policy",
      "requirements": [
        "RF-4.5",
        "RNF-T.2",
        "RNF-T.3"
      ],
      "dependsOn": [],
      "sectionAnchor": "slice-c1",
      "sources": [
        "SRC-PROTOCOL",
        "SRC-PATHS",
        "SRC-ATOMIC"
      ],
      "redCommands": [
        "CMD-POLICY"
      ],
      "greenCommands": [
        "CMD-POLICY",
        "CMD-TYPES",
        "CMD-NATIVE-TEST-BUILD",
        "CMD-NATIVE-FAULT"
      ],
      "reviewEvidence": [
        "specification",
        "code-quality"
      ],
      "risk": "full-context",
      "fallback": [
        "security",
        "root-configuration",
        "public-contract"
      ]
    },
    {
      "id": "C2",
      "title": "Runtime capabilities and role resolution",
      "requirements": [
        "RF-4.2",
        "RF-4.3",
        "RF-4.4",
        "RNF-T.1",
        "RNF-T.5"
      ],
      "dependsOn": [
        "C1"
      ],
      "sectionAnchor": "slice-c2",
      "sources": [
        "SRC-PROTOCOL",
        "SRC-ADMISSION"
      ],
      "redCommands": [
        "CMD-RESOLVE"
      ],
      "greenCommands": [
        "CMD-RESOLVE",
        "CMD-TYPES"
      ],
      "reviewEvidence": [
        "specification",
        "code-quality"
      ],
      "risk": "full-context",
      "fallback": [
        "security",
        "public-contract"
      ]
    },
    {
      "id": "C3",
      "title": "Compact v2 admission and honest forecast",
      "requirements": [
        "RF-4.1",
        "RF-5.2",
        "RNF-T.4"
      ],
      "dependsOn": [
        "C2"
      ],
      "sectionAnchor": "slice-c3",
      "sources": [
        "SRC-PROTOCOL",
        "SRC-PLAN",
        "SRC-ADMISSION"
      ],
      "redCommands": [
        "CMD-PLAN"
      ],
      "greenCommands": [
        "CMD-PLAN",
        "CMD-TYPES"
      ],
      "reviewEvidence": [
        "specification",
        "code-quality"
      ],
      "risk": "full-context",
      "fallback": [
        "public-contract",
        "identity"
      ]
    },
    {
      "id": "C4",
      "title": "Durable routing evidence and bounded recovery",
      "requirements": [
        "RF-6.1",
        "RF-6.2",
        "RNF-T.6",
        "RNF-T.7"
      ],
      "dependsOn": [
        "C3"
      ],
      "sectionAnchor": "slice-c4",
      "sources": [
        "SRC-PROTOCOL",
        "SRC-REQUESTS",
        "SRC-ADMISSION"
      ],
      "redCommands": [
        "CMD-JOURNAL"
      ],
      "greenCommands": [
        "CMD-JOURNAL",
        "CMD-TYPES"
      ],
      "reviewEvidence": [
        "specification",
        "code-quality"
      ],
      "risk": "full-context",
      "fallback": [
        "durable-state",
        "security",
        "public-contract"
      ]
    }
  ],
  "closureCommands": [
    "CMD-TYPES",
    "CMD-ALL"
  ]
}
<!-- AWM:COMPACT-SLICES:END v1 -->

## Requirements and ownership

The canonical brief IDs remain unchanged. CLI is the overall owner of the 15 IDs
in the manifest; registry consumer subcontracts use distinct R2B-B IDs and map
back to these canonical IDs. No requirement is owned by two slices.
Policy validation/storage is one boundary (C1); runtime resolution another (C2);
plan authority and forecast one boundary (C3); durable execution/recovery one
boundary (C4). This separation prevents new model routing from rewriting the
existing workflow or fingerprint algorithm.

## Shared execution contract

Read SRC-PROTOCOL completely. Its interfaces, bounded input algorithms,
persistence rules and command outputs are normative implementation facts.
Add `type VerifiedValidPlanReport = Extract<PlanValidationReport, { state: 'valid' }>`
to model-policy/types.ts, authenticate with existing
`assertVerifiedValidPlanReport` before resolution, and never accept a fabricated report.

Preparation at eventual execution: from isolated repo root run
`npm --prefix cli ci` once; build only tests that exercise cli/dist need it.
Establish one baseline and retain it until lockfile/runtime changes. This
planning session does not install dependencies or run product suites.

Create `cli/tests/fixtures/model-policy.ts` containing complete factories:
policy with one codex/native row (Luna medium, Terra medium, Sol medium,
full Sol high), all three degradation flags false, maxAttempts 3 and the
literal escalation/judgment effort arrays from SRC-PROTOCOL. Runtime fixture
is codex/native/version 1.0.0/accountScopeDigest = 64 lowercase a characters.
Receipt fixture supports each required routing capability, expires exactly
24h after a fixed timestamp, lists all four exact selections, and includes
matching native-control evidence receipt digests. Native receipts are fixture
evidence, never real platform certification. Tests advance an injected clock.

All RED commands must fail the named assertion for absent/incorrect behavior;
then GREEN must pass on the same facts. Imports missing before a new module is
created are initial setup, not the sole RED proof: also mutate the implemented
decision and prove the behavioral negative fails.

<a id="slice-c1"></a>
### Slice C1: Approved portable policy
#### Surfaces
Own RF-4.5, RNF-T.2, RNF-T.3 in new
`cli/src/core/model-policy/{types,validate,canonical,paths,store}.ts`,
`cli/src/commands/model-policy/index.ts`, registration in
`cli/src/index.ts`, and tests
`cli/tests/core/model-policy/{policy,store}.test.ts`,
`cli/tests/commands/model-policy/index.test.ts`, and
`cli/native/secure_fs.cc` with
`cli/tests/core/secure-fs/native-bridge.test.ts`, plus the test-only
build definition in `cli/native/binding.gyp` and `cli/package.json`.
Reuse SRC-PATHS and SRC-ATOMIC. This slice owns policy custody only;
capability attestation and routing are C2.

#### Implementation
- [ ] Write complete content/envelope guards and adversarial tests before writer implementation.
- [ ] Run CMD-POLICY RED: unknown/duplicate field, digest mismatch and failed CAS must reject.
- [ ] Implement the complete SRC-PROTOCOL PolicyContent/ApprovedPolicy interfaces and store API.
The content validator returns a validated object; no coercion, partial map,
default approval or reader writes. Candidate row must cover all profiles/full,
and target/runtime pairs are unique. Scope user/project chooses the exact path
in SRC-PROTOCOL; project-invalid never falls back.
- [ ] Implement this canonical hashing algorithm after strict validation:
```ts
function canonical(value: unknown): string {
  if (value === null || typeof value !== 'object') {
    const serialized = JSON.stringify(value);
    if (serialized === undefined) throw new Error('Invalid canonical value');
    return serialized;
  }
  if (Array.isArray(value)) return '[' + value.map(canonical).join(',') + ']';
  const object = value as Record<string, unknown>;
  return '{' + Object.keys(object).sort().map(
    key => JSON.stringify(key) + ':' + canonical(object[key])).join(',') + '}';
}
export function canonicalPolicyDigest(content: PolicyContent): string {
  const validated = validatePolicyContent(content);
  return createHash('sha256').update(canonical(validated), 'utf8').digest('hex');
}
```
Import createHash from node:crypto. JSON primitive return is safe because guards
have already excluded undefined/functions/nonfinite values.
- [ ] Implement exclusive writer lock, identity/content reread under lock,
expected predecessor comparison, fully validated write and durable publication.
Initial publication must not replace an existing policy. Preserve predecessor
when any validation/write/fsync fails; release only the lock this writer owns.
No stale-lock auto-deletion.
- [ ] Amend C1-2026-09-17: native secure-fs is part of the writer boundary.
After a successful publish or temporary cleanup, directory fsync failure must
throw a bounded error, never return publication success. Add source-level
regression for every supported host and native-artifact behavioral regression
where the CI prebuild is available. The model-policy store tests must exercise
the real bridge or an explicitly injected bridge that preserves its exact
identity-fenced transaction contract; mocks that bypass native fencing,
write, or fsync do not prove this requirement.
- [ ] Amend C1-2026-09-17 fault injection: compile a distinct addon only with
`AWM_SECURE_FS_TESTING=1` through a declared `native:test-build` command.
It may expose a narrowly named test control solely in that addon to force the
directory-sync error after publication. Production `native:build`, package
artifacts and runtime bindings must not export, invoke, or accept this control.
The artifact-backed test loads the test addon, proves publication occurred and
the call throws the bounded durability error; it then proves an ordinary
published prebuild has no test control. No environment variable, public CLI
flag, production configuration, or hidden runtime switch is allowed.
Run CMD-NATIVE-TEST-BUILD immediately before CMD-NATIVE-FAULT; the latter is
the behavioral evidence and must not pass merely because the addon compiled.
- [ ] Register contract/approve/status command scaffolding and dependency
injection for pure tests. Their argument validation and protocolDigest are
defined in SRC-PROTOCOL; expose unsupported capability state until C2.
- [ ] Run CMD-POLICY and CMD-TYPES GREEN; commit cohesive slice.

#### Edge cases
Required assertions: reordered object keys yield equal digest; reordered arrays
yield different digest; approval timestamp changes do not change content digest;
two CAS writers with same predecessor yield exactly one accepted replacement;
absent directory during read remains absent; malicious parent/final symlink
leaves destination untouched; oversized bytes and duplicate JSON keys reject;
invalid project file blocks despite valid user policy. Inject write/fsync errors
and verify original bytes remain or explicit failed publication status is
returned, never success. Do not claim directory fsync unavailable on Windows
gives additional CAS guarantees.

#### Evidence
SRC-PROTOCOL, SRC-PATHS, SRC-ATOMIC; CMD-POLICY and CMD-TYPES.
Specification and quality reviewers independently reconcile exact writer
effects, tests, empirical sensors, files and current CLI plan digest.
Receipts include complete ledger-entries or an explicit empty collection.
Do not dispatch a review to repair administrative-only omissions.

#### Fallback
Unsafe parent identity, filesystem primitive limitation, lock collision or
uncertain durable replacement expands relevant context and blocks that writer.
Amend/revalidate explicit interface changes; never relocate operator state into
a registry, hardcode this machine, or broaden cleanup scope.

<a id="slice-c2"></a>
### Slice C2: Runtime capabilities and role resolution
#### Surfaces
Own RF-4.2, RF-4.3, RF-4.4, RNF-T.1, RNF-T.5 in
`cli/src/core/model-policy/{capabilities,resolve}.ts`, C1 types/command,
`cli/src/core/admission/index.ts` execution capability consumption,
`cli/src/commands/plan/index.ts` v1 resolve command registration,
and `cli/tests/core/model-policy/{capabilities,resolve}.test.ts`.
Grouping is one runtime-attested decision boundary; no native dispatch API is added.

#### Implementation
- [ ] Write negative resolution tests and capability attestation fixtures. CMD-RESOLVE RED.
- [ ] Implement the runtime/policy/selection steps from SRC-PROTOCOL in order;
use pure resolveSelection with injected requested profile, role, runtime,
policy, receipt and clock. It is independent of plan schema. C3 owns v2
extraction and integration of the complete ten-step resolveDispatch algorithm.
- [ ] Keep AGENT_TARGETS exhaustive. Preserve R1 capability facts for v1.
Routed support comes only from matching explicit native receipt approval.
Docs-only or renderer evidence never changes supported.
- [ ] Implement capabilities approve using C1 exclusive/CAS writer machinery,
matching supported evidence, digest approval and strict clock/runtime/account.
- [ ] Complete status and plan resolve command inputs/output/exit codes.
Construct and hash frozen envelope only after every required gate.
- [ ] Resolve nonimplementer roles to full independently:
```ts
const profile = input.role === 'implementer' ? input.requestedProfile : 'full';
const selection = profile === 'full'
  ? mapping.fullCapability : mapping.profiles[profile];
```
C2 resolveDispatch authenticates only the existing v1 report: without opt-in
it returns not-required; with explicit opt-in it passes full to resolveSelection.
C3 adds the typed v2 slice and obtains its profile from the authenticated report.
No premature v2 casts, widened v1 keys or unvalidated field access are allowed.
- [ ] Run CMD-RESOLVE and CMD-TYPES GREEN; commit.

#### Edge cases
Test all six targets with fixture receipts, including blocked/unverified rows.
Different runtime kind/version/account blocks even for equal model IDs.
24h boundary inclusive, later stale; future timestamp/expiry over 24h rejects.
Missing override unsupported may degrade only to explicitly attested full
default; unverified blocks. Unknown model/effort or allowlist substitution
blocks. Missing observed identity needs explicit approved degradation and
savings unavailable. Spec/quality/QA/controller/docs/retro always full;
mechanical does not lower reviewer model. No available-selection alternative
is selected silently. V1 absent opt-in returns not-required without reading
or approving a personal policy.

#### Evidence
SRC-PROTOCOL and SRC-ADMISSION. CMD-RESOLVE tests behavior, not model names
found somewhere in prose. Distinct current spec/quality verdicts plus sensors.
Native receipts remain untested on real Claude; fixture PASS is not model acceptance.

#### Fallback
Missing real capability availability blocks only routed execution. Continue
implementation using explicit fixtures; do not launch paid probes or certify
actual platform support. Schema/public/security changes use full relevant
context with R2-A fix grouping.

<a id="slice-c3"></a>
### Slice C3: Compact v2 admission and honest forecast
#### Surfaces
Own RF-4.1, RF-5.2, RNF-T.4 in
`cli/src/core/plan/{types,validate,identity}.ts`,
`cli/src/core/admission/{index,registry-contracts}.ts`,
`cli/src/commands/plan/index.ts`, and existing
`cli/tests/core/plan/{validate,identity}.test.ts`,
`cli/tests/core/admission/index.test.ts`,
`cli/tests/commands/plan/index.test.ts`.
One parser/identity/ordered admission boundary; no skill parser.

#### Implementation
- [ ] Add real v2 fixtures for three profiles, v1 corpus and rejected concrete
model/profile keys. Run CMD-PLAN RED against v2 behavioral expectations.
- [ ] Replace manifest/slice types with this discriminated extension:
```ts
export interface PlanSliceV2 extends PlanSlice {
  implementerProfile: 'mechanical' | 'integration' | 'judgment';
}
type CompactPlanV2 = Omit<CompactPlanManifest, 'schema' | 'slices'> & {
  schema: 'compact-slices/v2'; slices: PlanSliceV2[];
};
type SupportedPlanManifest = CompactPlanManifest | CompactPlanV2;
```
Keep PlanSlice and CompactPlanManifest v1 intact, then update valid report to
the supported discriminated union. Strict exact keys depend on schema.
Markers v1/v2 must match manifest schema; five subsections remain unchanged.
Future v3 remains unsupported. Never weaken markerless/unknown-field rejection.
- [ ] Extend identity scanner to recognize both exact manifest boundaries;
status/marker normalization never strips v2 profiles or model-policy prose.
All profile changes alter full/execution identity.
- [ ] Complete resolve command delegation to verified plan plus C2 resolver.
For v2 admission add policy/capability gates after existing currentness/sensors/
journal ordering. Missing required routing facts blocks with no dispatch.
V1 without opt-in follows unchanged R1 admission and forecast.
- [ ] Add routingForecast per SRC-PROTOCOL. Admission optionally receives
explicit validated QA lens IDs; globals/profiles/roles counted independently.
Unknown Track B/controller count is unavailable with lower bound, never zero
or arbitrary one. Preserve old forecast field for v1 consumers.
- [ ] Run CMD-PLAN and CMD-TYPES GREEN; commit.

#### Edge cases
V1 any implementerProfile rejects; v2 missing/two profiles/concrete model/unknown
profile rejects. All three valid v2 profiles accept. Six targets unknown/disabled
same behavior as existing gates. Markerless future schema classification remains
unsupported; historical original bytes never mutated. New profile or manifest
schema changes digest; lifecycle progress bytes preserve execution identity
only under current approved identity contract. Missing QA lens count cannot
yield an exact total. Snapshot swapped after validation cannot supply resolution.

#### Evidence
SRC-PROTOCOL, SRC-PLAN, SRC-ADMISSION; CMD-PLAN and CMD-TYPES.
Current independent spec/quality evidence must include v1 nonregression,
v2 schema and ordered zero-dispatch negatives. Validation is not admission.
The bootstrap implementation plan itself remains v1 throughout.

#### Fallback
If a changed profile/policy/capability invalidates an obligation, use current
admission and explicit rebind semantics; never rewrite historical plans or
transfer old PASS. Expand full relevant context for parser/identity uncertainty.

<a id="slice-c4"></a>
### Slice C4: Durable routing evidence and bounded recovery
> **Implementation status (2026-09-17):** C1–C4 code and automated evidence
> were implemented on the issue branch. The checklist below remains authored
> acceptance criteria, not proof of native/provider acceptance; native and
> Claude execution are explicitly UNTESTED pending the playbook.
#### Surfaces
Own RF-6.1, RF-6.2, RNF-T.6, RNF-T.7 in new
`cli/src/core/model-policy/journal.ts`,
`cli/src/core/journal/{types,requests,store}.ts`,
`cli/src/commands/watch/apply.ts`,
`cli/src/commands/job/{index,gate,export}.ts`,
new `cli/tests/core/model-policy/journal.test.ts`,
`cli/tests/commands/job/routing.test.ts`, existing journal/apply/gate tests,
and `docs/acceptance/r2b-native-routing.md`.
One supervisor custody boundary: attempts, observations, verdict references,
recovery budget and report. No second workflow journal.

#### Implementation
- [ ] Add state/requests/ack/replay and interlock behavioral assertions;
run CMD-JOURNAL RED for stale routing evidence and exhausted budget.
- [ ] Add optional typed routingAttempts and implementationLineages with
recursive guards and unknown-field rejection, bounded collection max 4096.
Legacy absence readable but never satisfies routed obligations.
- [ ] Implement pure helpers in model-policy/journal.ts:
```ts
reserveRoutingAttempt(state: JournalState, input: {
  obligationId: string; lineageId: string; envelope: DispatchEnvelope;
  fingerprint: string }, now: string): { state: JournalState; attemptId: string };
observeRoutingAttempt(state: JournalState, input: {
  attemptId: string; nativeAgentId: string; observed?: Selection;
  unavailableReason?: string }, now: string): JournalState;
```
They validate a cloned state and return only a complete valid replacement.
Attempt IDs hash lineage/ordinal/envelope digest/fingerprint. Existing active
equivalent attempt is returned; new equivalent reservation cannot reset count.
Implementer reservations consume max 3; other role counts are separately
measured and retain current obligations. Explicit mismatch marks attempt
blocked and invalidates only linked current evidence.
- [ ] Add routing-reserve/observe RequestKind variants, supervised reducer and
commands from SRC-PROTOCOL. Include generation/envelope/fingerprint in
idempotency key; applied ack must precede native action. Unknown outcome
requires native custody reconciliation instead of blind redispatch.
- [ ] Complete plan resolve --lineage with a validated bound journal read.
Use SRC-PROTOCOL's exact initial-profile/escalation/effort sequence, record
effective escalation without editing the plan, and recheck selection, budget
and bound slice/obligation in the reservation reducer. Before C4, any lineage
request is blocked as unavailable rather than ignored.
- [ ] Attach routed attempt ID to routed verdicts/verification records only.
Extend gate fail-closed for missing/stale/mismatched routing evidence; ordinary
mechanical jobs and v1 verdicts retain original fingerprint/gate semantics.
Rebind cannot erase lineage or certify old routed verdicts.
- [ ] Implement read-only routing-report with planned/actual per role,
attempts, retries, fallback and unavailable provenance; no bodies/prompts/
credentials. Add focused interruption/cross-generation tests.
- [ ] Write native acceptance playbook: exact CLI/registry pair, scratch
HOME/AWM_HOME/cwd, approved policy+native capability receipts, mechanical and
integration actual native agent IDs/effective selection/full independent
reviews; unavailable Claude remains UNTESTED and other uncertified targets
blocked/degraded. Rollback retains reports outside fixture and restores no
global machine configuration.
- [ ] Run CMD-JOURNAL and CMD-TYPES GREEN; commit.

#### Edge cases
Reserve duplicate replay returns one attempt; changed digest under same key
rejects; stale generation cannot reserve/observe. Restart, steering, new
planDigest or generation never resets three-attempt lineage. Fourth
implementation reservation rejects and gate blocks with pending finding.
A network/currentness/admin omission cannot create new implementation attempt.
Crash between reserve ack and dispatch has unknown outcome until native
reconciliation; do not adopt an agent by title or PID alone.
Actual model or effort mismatch invalidates linked review despite PASS.
Opaque response/native prompt bodies in receipt reject. Missing observability
degrades only approved flags and cannot certify savings. Test read commands
before/after filesystem inventory, injected atomic failure and historical
schema records. Native Windows custody gap remains honest, separate from
sensor-certification PASS.

#### Evidence
SRC-PROTOCOL, SRC-REQUESTS, SRC-ADMISSION. CMD-JOURNAL and CMD-TYPES, then
CMD-ALL once for the final candidate. Distinct final review, QA Track A and
applicable Track B, docs, retro and empirical sensors retain R2-A cycle control.
Record exact source SHA and tests; fixture evidence cannot become native PASS.
Link material artifacts/commits/PRs/checkpoints from issue #126 at approved
publication; this planning request alone does not post external comments.

#### Fallback
Missing native agent identity/custody or ambiguous outcome blocks the affected
reservation; do not duplicate it. Missing runtime acceptance blocks its
operational enablement, not other implementation slices. Failures classified
environment/currentness/publication/persistence rerun only the affected gate.
Any new contract decision requires amendment, CLI validation and current
independent verdicts.

## Traceability and closure

| Requirement owner | Assertions that prove the claim |
| --- | --- |
| C1: RF-4.5 | malformed/unapproved policy, CAS/digest rejection, zero writer success |
| C1: RNF-T.2 | bounded unknown input, duplicate fields, unsafe paths/symlinks |
| C1: RNF-T.3 | operator-local atomic state, redacted bounded records, no reader write |
| C2: RF-4.2 | exact available profile selection from approved policy |
| C2: RF-4.3 | every nonimplementer resolves independently to full |
| C2: RF-4.4 | unsupported override approves only visible full degradation |
| C2: RNF-T.1 | all six targets; renderer/docs never certify runtime |
| C2: RNF-T.5 | missing selection/observability/override yields named degradation/block |
| C3: RF-4.1 | three profiles accept; missing/concrete/unknown rejects; v1 untouched |
| C3: RF-5.2 | role/profile forecast and unavailable QA/controller count |
| C3: RNF-T.4 | v1 originals unchanged and v2 identity mutation proof |
| C4: RF-6.1 | acceptance/baton records exact SHA/plan identity/PR paths |
| C4: RF-6.2 | planned/actual roles, retries/fallbacks and unavailable fields |
| C4: RNF-T.6 | stale routed PASS interlock; all quality roles remain required |
| C4: RNF-T.7 | replay/fencing/restart/unknown outcome/attempt lineage negatives |

No UI surfaces. No pricing model, quota percentages or R3 savings experiment.
The policy matrix/budget is proposed in SRC-PROTOCOL for review before
implementation, never silently approved by an installer.

Eventual final sequence: C1 -> C2 -> C3 -> C4 -> final review -> global QA ->
docs -> retro -> final verification -> PR against main. Do not initialize
journal or offer unattended execution during authoring. At execution handoff
run strict currentness, empirical sensors, context budget, validate and admit
the exact plan; missing custody remains blocked. R8 registry exception is not
a waiver for CLI sensors.

Release this CLI through its existing conventional-commit publisher; leave
package version bump to the publisher, do not pre-bump and create a double minor.
Expected additive release is 9.9.0, but observe the actual published version.
Registry B3 uses that exact compatible release as minCliVersion only after
real installed acceptance. Preserve baseline v4.1.0 until registry release is
independently certified. Global environment updates require the owner's request.
